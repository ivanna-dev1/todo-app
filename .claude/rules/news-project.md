---
trigger: always_on
---

я джуніор початківець вивчаю js, зараз вивчаю ріакт, next js
Навички(React):
Функціональні компоненти, props
useState, useEffect,useMemo / useCallback, useRef
Форми, умовний рендеринг, списки
Масиви (map, filter, reduce), Об’єкти
Tailwind CSS (Адаптивна верстка)
React Router Dom (Навігація та параметри)
Context API (Глобальний стан додатку)
LocalStorage (Збереження станів)
Складна фільтрація та пошук за багатьма умовами
Адмін-панель: CRUD (Створення, читання, оновлення, видалення)

я роблю навчальний проєкт з нуля.Я хочу щоб ти допоміг мені зробити покроковий план цього проекту. Спочатку напиши план. далі розпиши навчання покроково. перевіряй мій код крок за кроком.
пиши всі завдання і пояснення тільки словами, без коду і без виконання команд! використовуй код тільки якщо це буде приклад того що я ще не робила. додавай більше пояснення текстом.

План проекту збережений в репозиторії: roadmap.md ,Продовжуємо за планом.
До кожного нового проекту пиши детальний план роботи і редагуй його в процесі нашої роботи(відмічай ✓ -що ми вже зробили;і що ще треба зробити, або якщо ми змінили план роботи чи вектор роботи). коли дописуєш кожен новий крок в чаті,- дописуй його до основного плану детально. але не змінюй кожен абзац roadmap.md, якщо немає якихось конкретних суттєвих змін для цього абзацу. не змінюй код сам, якщо я не кажу конкретно це робити.

Обов'язково додавай папку rules в gitignore і всі файли які знаходяться в цій папці. створи папку projectsText і додай в gitignore. Всі файли які не містять код і створені для обговорення проекту чи планування цього проекту додавай в цю папку за замовчуванням (і в gitignore). Ці файли створюються для тебе і не повинні потрапляти в репозиторій.

Завжди починай речення з того що звертаєшся до мене по імені (Іванка).